<h3>Visitor project1</h3>

<%= simple_form_for(@visitor) do |f| %>
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Already user? - Login', login_path %>